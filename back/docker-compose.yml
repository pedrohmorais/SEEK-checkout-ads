version: '3'
services:
  app-backend:
    command: npm start
    build: .
    ports:
     - "3000:3000"
    links:
        - mongodb
    depends_on:
        - mongodb
  mongodb:
    image: mongo:latest
    container_name: "mongodb"
    ports:
      - 27017:27017